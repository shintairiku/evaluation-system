<!-- docs/implementation-storategy/0127-comprehensive-evaluation/03-frontend-mock-summary.md -->
# 総合評価（管理者向け）フロントエンド・モック実装まとめ（ステークホルダー確認用）

## リビジョン履歴

| バージョン | 日付 | 変更者 | 変更内容 |
|---|---|---|---|
| v1.0 | 2026-01-28 | AI（Codex） | フロントエンドのモック実装範囲を要約 |
| v1.1 | 2026-02-02 | AI（Codex） | 2. 総合評価テーブル一覧ページ - 正社員/パート列の追加 <br>2. 判定ルール設定UI - 特例ステージ・レベル増減設置 <br>5.1 フィルタ/検索 - 正社員/パート＆未処理フィルターの追加 |
| v1.2 | 2026-02-02 | AI（Codex） | 判定ルールの要件として `AND`/`OR` 条件を明記（UI/Backendの実装計画に反映） |

---

## 1. 本資料の目的

ステークホルダー承認前の **マイルストーン1（フロントのみ・モックデータ）** として、現在UIで確認できる機能・ルール・画面を1つの資料にまとめたものです。

---

## 2. 実装スコープ（マイルストーン1）

### 2.1 実装済み（フロントのみ）

- 管理者向け「総合評価テーブル」の一覧ページ（モック表示）
- `eval_admin` 向けの「判定ルール設定（モック）」UI（ブラウザ内保存）
- 昇格/降格候補者の一覧ページ（モック表示）
  - `eval_admin` が「面談/プレゼン/CEO面談クリア」フラグをチェックで更新可能（ブラウザ内保存）
  - `eval_admin` が「特例反映」として判定/ステージ/レベルを手動で上書き可能（理由・ダブルチェック者必須、ブラウザ内保存）

### 2.2 未実装（承認後に実装）

- Backend / API
- DB（`evaluation_score_mapping`等）との接続、権限の厳密運用
- 年間（1年）評価の集約（上期/下期を跨いだ判定）
- スプレッドシートと同等の完全なデータ連携（#N/Aの発生条件含む）
- ステージ/レベルと給与（基本給/時給）の自動算出・表示
  - 仕様: `docs/implementation-storategy/0127-comprehensive-evaluation/04-stage-level-compensation.md`

---

## 3. 対象ページ（URL）

- 総合評価テーブル（一覧）: `/admin-eval-list`
- 昇格/降格候補（一覧）: `/admin-eval-list/candidates`

---

## 4. ロールと権限（フロント側ガード）

### 4.1 閲覧

- `admin` / `eval_admin` のみ閲覧可能（管理者相当）

### 4.2 編集（判定ルール・面談フラグ）

- `eval_admin` のみ編集可能
  - `/admin-eval-list` の「判定ルール設定」ボタン表示
  - `/admin-eval-list/candidates` の面談フラグ（チェックボックス）操作

---

## 5. 総合評価テーブル（/admin-eval-list）の表示内容（モック）

### 5.1 フィルタ/検索

- 評価期間（必須）
- 部署
- ステージ
- 雇用形態（正社員・パート）
- 処理状態（未処理 など）
- テキスト検索（社員番号・氏名・部署・ステージ）
- クリアボタン

### 5.2 主な列（スプレッドシート相当）

- 個人基本情報: 社員番号 / 氏名 / 部署 / 雇用形態（正社員・パート）
- 目標達成（定量+定性）: 最終評価 / ウェイト / 点数
- コンピテンシー: 最終評価 / ウェイト / 点数
- コアバリュー: 最終評価
- 総合結果: 合計（点）/ 総合評価 / MBO D評価フラグ /（判定）/ レベル増減 / 現在ステージ / 現在レベル / 新レベル（31以上はアラート）

---

## 6. 算出・判定ロジック（モック実装）

### 6.1 合計点

- `合計点 = 業績点 + コンピテンシー点`
- どちらかが未確定（`null`）の場合は、合計点は未表示（`-`）

### 6.2 総合評価（合計点 → ランク）

合計点から、以下の「＜総合評価・点数対応表＞」で **総合評価（SS〜D）** を決定します。

| 総合評価 | 点数レンジ |
|---|---|
| SS | 6.5点以上 |
| S | 5.5点以上 〜 6.5点未満 |
| A+ | 4.5点以上 〜 5.5点未満 |
| A | 3.7点以上 〜 4.5点未満 |
| A- | 2.7点以上 〜 3.7点未満 |
| B | 1.7点以上 〜 2.7点未満 |
| C | 1.0点以上 〜 1.7点未満 |
| D | 0.1点以上 〜 1.0点未満 |

### 6.3 MBO D評価フラグ（総合評価の上書き）

- 設定が有効な場合、`MBO D評価フラグ = 1` のとき、総合評価は **Dに上書き** します
  - 前提: `MBO D評価フラグ`は「同一年度の上期と下期のMBOが両方D」のときに `1` となる年次判定用フラグ
  - 背景ルール: 「上期と下期のMBOが両方Dの場合、コンピテンシー/クレドにかかわらず（年次の）総合評価はD」
- 注: 上の総合評価テーブルは半期ごとの評価を表す想定のため、年次集約が未実装のマイルストーン1ではフラグはモック値（表示のみ）として扱います

### 6.4 昇格判定（昇格候補フラグ）

昇格判定は固定フォームではなく、**`AND`/`OR`を含むルール式**として設定できること。

ルールで扱える条件（例）:
- 総合評価が指定ランク以上
- コンピテンシー最終評価が指定ランク以上
- クレド（コアバリュー）最終評価が指定ランク以上
- 面談/プレゼン/CEO面談が必須（ON/OFF）

例（イメージ）:
- `総合評価がA+以上 AND コンピテンシーがA+以上`
- `総合評価がS以上 OR (総合評価がA以上 AND 面談3つすべてクリア)`

※ 本判定は「候補/フラグ」を意味し、ステージ（グレード）の自動更新は行いません（別途、人事確定プロセスで反映）。

### 6.5 降格判定（降格候補フラグ）

- 年間の総合評価が `D` の場合、判定は **降格** になります（しきい値は設定で変更可能）
- 注: マイルストーン1では「年間」の集約は未実装のため、当該行の総合評価（モック）で代用しています
  - 本判定も「候補/フラグ」を意味し、ステージ（グレード）の自動更新は行いません

### 6.6 レベル増減と新レベル

- レベル増減
  - **総合評価（SS〜D）に応じたデフォルト増減** を適用（設定で変更可能）
  - パートタイムにはレベルの概念がないため、レベル増減は `-` 表示とする

| 総合評価 | レベル増減（デフォルト） |
|---|---:|
| SS | +10 |
| S | +8 |
| A+ | +6 |
| A | +5 |
| A- | +2 |
| B | +1 |
| C | -5 |
| D | -8 |

- 新レベル
  - `新レベル = 現在レベル + レベル増減`
  - `新レベル >= 31` の場合、アラート表示（強調）
  - パートタイムは `現在レベル/新レベル` ともに `-` 表示（アラート対象外）

---

## 7. eval_admin向け「判定ルール設定」（/admin-eval-list）

### 7.1 変更できる項目（モック）

- 昇格条件
  - **`AND`/`OR`で組めるルールビルダー**（ORグループ/AND条件など）
  - ルール条件: 総合評価（最低ランク）、コンピテンシー（最低ランク）、クレド（最低ランク）、面談/プレゼン/CEO面談の必須（ON/OFF）
- 降格条件
  - **`AND`/`OR`で組めるルールビルダー**（ORグループ/AND条件など）
  - ルール条件: 年間総合評価（降格しきい値）、MBO D評価フラグで総合評価をDに上書きするか（ON/OFF）
- レベル増減（総合評価別）
  - SS/S/A+/A/A-/B/C/D それぞれの増減値

### 7.2 保存方法（重要）

- 【マイルストーン1（モック）】設定は **ブラウザ（localStorage）に保存** されます（デモ/レビュー用）
  - サーバーやDBには保存されません（他ユーザー/他端末へ共有されません）
- 【本番（Production想定）】`eval_admin`による設定変更は **サーバー/DBに永続化** し、組織内で共有される必要があります
  - 想定DB:
    - `evaluation_score_mapping`（点数→ランク、総合評価別`level_delta`等）
    - 複合条件ルール（`AND`/`OR`）用の別テーブル（例: `comprehensive_evaluation_rules`）

---

## 8. 昇格/降格候補ページ（/admin-eval-list/candidates）

### 8.1 目的

昇格/降格の判定ルールに基づき、候補者を一覧表示し、必要なクリア条件（面談等）の状態を確認・更新できるようにします。

### 8.2 機能（モック）

- フィルタ/検索（評価期間・部署・ステージ・雇用形態（正社員・パート）・候補種別・検索）
- 「候補者のみ」トグル（対象外を非表示）
- 昇格候補数 / 降格候補数の表示
- 一覧上で雇用形態がぱっと見て判別できるように表示（例: 正社員/パートのバッジ）
  - パートタイムにはレベルの概念がないため、レベル関連（現在レベル/レベル増減/新レベル）は `-` 表示（アラート対象外）
- `eval_admin`のみ:
  - リーダー面談 / 事業部長プレゼン / CEO面談のチェック更新（マイルストーン1ではブラウザ保存。本番ではDB保存）
  - 特例反映（手動上書き）
    - 判定（昇格/降格/対象外）とステージ増減、レベル増減（正社員のみ）を入力して上書き
    - 理由とダブルチェック者が必須（モックではブラウザ保存。本番では監査ログ含めDB保存）

---

## 9. 次マイルストーン（承認後）の実装ポイント（提案）

- `evaluation_score_mapping`の設計確定（「＜最終評価・点数対応表＞」と「＜総合評価・点数対応表＞」の保持方法）
  - 「総合評価（SS〜D）別のレベル増減（SS:+10 / S:+8 / A+:+6 / A:+5 / A-:+2 / B:+1 / C:-5 / D:-8）」は、`evaluation_score_mapping`の「＜総合評価・点数対応表＞」に`level_delta`として保持する想定
- 年間判定（上期+下期の集約）をバックエンド集計で一貫実装
- クリア条件（面談/プレゼン/CEO面談）の永続化（DB保存）と監査ログ
- 特例反映（手動上書き）の永続化（DB保存）と監査ログ（誰が・いつ・なぜ・ダブルチェック者）
- 権限（`admin`閲覧のみ / `eval_admin`編集可）の正式運用（API側で強制）
- 判定ルール（昇格/降格/上書き）を `AND`/`OR` を含む条件式として永続化し、Backendで評価する
  - `evaluation_score_mapping`（点数→ランク/デフォルト増減）とは責務を分離し、ルール定義は別テーブル（JSONB等）で持つ想定
